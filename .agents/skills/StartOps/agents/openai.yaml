interface:
  display_name: "StartOps"
  short_description: "Repo-specific patterns and workflows for StartOps"
  default_prompt: "Use the StartOps repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true